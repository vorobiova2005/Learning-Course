//🎮 Симуляція: «Підрахунок бюджету»**
// Є об'єкт `expenses` (Витрати), де ключі — категорії («Їжа», «Таксі»), а значення — суми.
// Використовуючи цикл, порахуй загальну суму витрат за місяць.

const expenses = {
    food: 200,
    taxi: 300
}

let total = 0

for (const key in expenses){
    total += expenses[key]
}
console.log(total)
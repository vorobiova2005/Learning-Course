/*
╔══════════════════════════════════════════════════════════════╗
║   ЗОРЯНІ ХРОНІКИ: ШЛЯХ КОДЕРКИ                              ║
║   🔮 Розділ 11: АНАЛІЗ ГАЛАКТИКИ                            ║
║   Тема: map, filter, reduce, find, every, some, sort         ║
╚══════════════════════════════════════════════════════════════╝

  🌌 СЮЖЕТ

  Луна стоїть перед Галактичним Центром Аналізу Даних.
  Тут зберігаються мільйони записів про флот, джедаїв,
  та операції Темного Компілятора.

  "Перед Фінальною Битвою нам потрібні дані!" —
  кричить адмірал R2-JS. "Треба обробити масиви
  із тисячами записів — ШВИДКО і ЕЛЕГАНТНО!"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  📚 ТЕОРІЯ: ВИЩІ МЕТОДИ МАСИВІВ

  map(fn)    — трансформує КОЖЕН елемент:
    [1,2,3].map(x => x * 2) → [2,4,6]

  filter(fn) — фільтрує, залишає тільки ті де fn → true:
    [1,2,3,4].filter(x => x > 2) → [3,4]

  reduce(fn, initial) — зводить до одного значення:
    [1,2,3].reduce((acc, x) => acc + x, 0) → 6

  find(fn)   — перший елемент де fn → true:
    [{id:1},{id:2}].find(x => x.id === 2) → {id:2}

  findIndex(fn) — індекс першого відповідного (-1 якщо немає)

  every(fn)  — true якщо ВСІ відповідають умові
  some(fn)   — true якщо ХОЧА Б ОДИН відповідає

  sort((a,b) => a-b)  — сортування з comparator

  flat()     — розгладжує вкладені масиви

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*/

// ТЕСТОВІ ДАНІ (використовуй у своїх функціях як аргументи)
const fleet = [
  { name: 'X-Wing',           power: 85,  type: 'fighter',   fuel: 80 },
  { name: 'Y-Wing',           power: 60,  type: 'bomber',    fuel: 40 },
  { name: 'Millennium Falcon',power: 95,  type: 'freighter', fuel: 100 },
  { name: 'A-Wing',           power: 70,  type: 'fighter',   fuel: 90 },
  { name: 'B-Wing',           power: 55,  type: 'bomber',    fuel: 30 },
  { name: 'Ghost',            power: 75,  type: 'freighter', fuel: 65 },
];

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.1 — MAP: СПИСОК ІМЕН
// ═══════════════════════════════════════════════════════════
/*
  Отримай масив ТІЛЬКИ імен кораблів.
  Використай .map()
*/

function imenaKorabliv(ships) {
  return ships.map(???);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.2 — MAP: АПГРЕЙД ФЛОТУ
// ═══════════════════════════════════════════════════════════
/*
  Збільш потужність (power) КОЖНОГО корабля на 10%.
  Поверни НОВИЙ масив об'єктів (оригінал не змінювати!).
  Округли до цілих (Math.round).
*/

function aphreidFlotu(ships) {
  return ships.map(ship => ({
    ...ship,
    power: ???,
  }));
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.3 — FILTER: ТІЛЬКИ ВИНИЩУВАЧІ
// ═══════════════════════════════════════════════════════════
/*
  Відфільтруй тільки винищувачі (type === "fighter").
*/

function tilkyVynyschuvachy(ships) {
  return ships.filter(???);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.4 — FILTER: БОЙОВІ КОРАБЛІ
// ═══════════════════════════════════════════════════════════
/*
  Залиш кораблі з:
  - power >= minPower
  - fuel >= minFuel
*/

function boiezdatniKorabli(ships, minPower, minFuel) {
  return ships.filter(???);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.5 — REDUCE: ЗАГАЛЬНА ПОТУЖНІСТЬ
// ═══════════════════════════════════════════════════════════
/*
  Порахуй СУМУ потужності всіх кораблів.
  Використай .reduce()
*/

function zahalnaPotuzhtnist(ships) {
  return ships.reduce(???, 0);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.6 — FIND: ЗНАЙДИ КОРАБЕЛЬ
// ═══════════════════════════════════════════════════════════
/*
  Знайди корабель за іменем. Поверни весь об'єкт або null.
*/

function znaidyKorabl(ships, name) {
  return ships.find(???) ?? null;
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.7 — EVERY / SOME: ГОТОВНІСТЬ ФЛОТУ
// ═══════════════════════════════════════════════════════════
/*
  chyVsiHotovi(ships)  → true якщо fuel >= 50 у ВСІХ кораблів
  chyKhtosMaie(ships)  → true якщо ХОЧА Б ОДИН має power >= 90
*/

function chyVsiHotovi(ships) {
  return ships.every(???);
}

function chyKhtosMaie(ships) {
  return ships.some(???);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.8 — SORT: РЕЙТИНГ ФЛОТУ
// ═══════════════════════════════════════════════════════════
/*
  Відсортуй кораблі за потужністю (power) від БІЛЬШОГО до МЕНШОГО.
  Поверни НОВУ копію (не змінюй оригінал).
*/

function reitynh(ships) {
  return [...ships].sort(???);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.9 — ЛАНЦЮЖОК МЕТОДІВ (БОНУС ⭐)
// ═══════════════════════════════════════════════════════════
/*
  Ланцюжок (method chaining) — виклики методів по черзі.

  Отримай список імен найсильніших винищувачів (power >= 70),
  відсортованих від найсильнішого, у вигляді рядків: "X-Wing (85)"

  1. filter: type === "fighter"
  2. filter: power >= 70
  3. sort: від більшого до меншого
  4. map: перетворити на рядок "[name] ([power])"
*/

function topVynyschuvachy(ships) {
  return ships
    .filter(???)
    .filter(???)
    .sort(???)
    .map(???);
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 11.10 — REDUCE: ГРУПУВАННЯ (СУПЕР-БОНУС ⭐⭐)
// ═══════════════════════════════════════════════════════════
/*
  Згрупуй кораблі за типом (type).
  Результат: { "fighter": [...], "bomber": [...], "freighter": [...] }

  Використай reduce.
*/

function hrubovannia(ships) {
  return ships.reduce((groups, ship) => {
    ???
  }, {});
}

/*
  "АНАЛІЗ ЗАВЕРШЕНО!" — R2-JS тріумфально піщить.
  "У нас є всі дані для атаки!

  Луно... Настала ФІНАЛЬНА БИТВА.
  Темний Компілятор чекає на тебе.
  Використай ВСЕ що вивчила!"

  ➡️  Запусти: node chapter-11-vyshchi-metody/testy.js
*/

// ⚠️  НЕ ВИДАЛЯЙ!
module.exports = {
  fleet,
  imenaKorabliv,
  aphreidFlotu,
  tilkyVynyschuvachy,
  boiezdatniKorabli,
  zahalnaPotuzhtnist,
  znaidyKorabl,
  chyVsiHotovi,
  chyKhtosMaie,
  reitynh,
  topVynyschuvachy,
  hrubovannia,
};

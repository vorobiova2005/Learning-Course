/*
╔══════════════════════════════════════════════════════════════╗
║   ЗОРЯНІ ХРОНІКИ: ШЛЯХ КОДЕРКИ                              ║
║   ⚡ Розділ 7: ПАСТКИ ТЕМРЯВИ                               ║
║   Тема: Обробка помилок — try/catch/finally, throw, Error    ║
╚══════════════════════════════════════════════════════════════╝

  🌌 СЮЖЕТ

  Луна увійшла до Темного Сектору — найнебезпечнішої зони
  Галактичного Сервера. Тут Темний Компілятор розставив
  пастки — BUG-BОМБИ, які вибухають, якщо ти не готова!

  "Кожна пастка — це ПОМИЛКА в коді," — попереджає R2-JS.
  "Якщо не зловити помилку — програма впаде!
   Навчися їх ЛОВИТИ і ОБРОБЛЯТИ!"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  📚 ТЕОРІЯ: ОБРОБКА ПОМИЛОК

  try / catch / finally:
    try {
      // небезпечний код
    } catch (error) {
      // якщо виникла помилка — обробляємо тут
      console.log(error.message);
    } finally {
      // виконується ЗАВЖДИ (і при помилці, і без)
    }

  Типи вбудованих помилок:
    Error         — загальна помилка
    TypeError     — неправильний тип
    RangeError    — значення поза межами
    ReferenceError — невідома змінна

  Кинути помилку вручну:
    throw new Error("Щось пішло не так!");
    throw new TypeError("Очікувався рядок!");

  Кастомний клас помилки:
    class GalacticError extends Error {
      constructor(message) {
        super(message);
        this.name = "GalacticError";
      }
    }

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*/

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 7.1 — БЕЗПЕЧНИЙ ДІЛИТЕЛЬ
// ═══════════════════════════════════════════════════════════
/*
  Ділення на нуль — одна з пасток Темного Компілятора!

  Напиши функцію bezpechneDialennia(a, b):
  - Якщо b === 0 → кинь new Error("Ділення на нуль неможливе!")
  - Інакше → поверни a / b
*/

function bezpechneDialennia(a, b) {
  ???
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 7.2 — БЕЗПЕЧНИЙ ВИКЛИК
// ═══════════════════════════════════════════════════════════
/*
  Обгорни виклик функції у try/catch.

  bezpechnyiVyklyk(fn) отримує функцію fn.
  - Якщо fn() не кидає помилку — повертає її результат
  - Якщо fn() кидає помилку — повертає рядок:
    "Помилка: [повідомлення]"
*/

function bezpechnyiVyklyk(fn) {
  ???
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 7.3 — ВАЛІДАТОР ДЖЕДАЯ
// ═══════════════════════════════════════════════════════════
/*
  Перевір дані нового Джедая перед реєстрацією.
  Кидай помилки якщо:
  1. name не рядок або порожній → throw new TypeError("Ім'я має бути непорожнім рядком")
  2. power < 1 або power > 100  → throw new RangeError("Сила має бути від 1 до 100")
  3. typeof rank !== "string"   → throw new TypeError("Ранг має бути рядком")

  Якщо всі перевірки пройдено → повертай true
*/

function validateDzhedaia(name, power, rank) {
  ???
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 7.4 — КАСТОМНА ПОМИЛКА: GALACTIC ERROR
// ═══════════════════════════════════════════════════════════
/*
  Створи власний клас помилки GalacticError.
  Він розширює Error і має:
  - name === "GalacticError"
  - code — додаткова властивість (задається в конструкторі)

  Приклад:
    const err = new GalacticError("Втрачено зв'язок", "CONN_LOST");
    err.name    → "GalacticError"
    err.message → "Втрачено зв'язок"
    err.code    → "CONN_LOST"
*/

class GalacticError extends Error {
  constructor(message, code) {
    ???
  }
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 7.5 — FINALLY: ЗАКРИТИ КАНАЛ
// ═══════════════════════════════════════════════════════════
/*
  Після будь-якого зв'язку треба закрити канал (finally).

  Функція zviazok(message):
  - Відкриває "канал" (channel = "відкритий")
  - Намагається відправити message:
    * якщо message — порожній рядок: throw new Error("Порожнє повідомлення")
  - У finally: channel = "закритий"
  - Повертає об'єкт { success, channel, error }:
    * success: true (якщо без помилки) або false
    * channel: фінальний стан "закритий"
    * error: повідомлення помилки або null
*/

function zviazok(message) {
  let channel = 'відкритий';
  let success = false;
  let errorMsg = null;

  ???

  return { success, channel, error: errorMsg };
}

// ═══════════════════════════════════════════════════════════
// 🎯 ЗАВДАННЯ 7.6 — ЗОНА НЕБЕЗПЕКИ (БОНУС ⭐)
// ═══════════════════════════════════════════════════════════
/*
  Функція zonaNebezpeky(operations) отримує масив операцій.
  Кожна операція — це функція, що може кинути помилку.

  Виконай кожну операцію і:
  - Якщо успіх → записати {success: true, result: результат}
  - Якщо помилка → записати {success: false, error: error.message}

  Поверни масив результатів.
*/

function zonaNebezpeky(operations) {
  ???
}

/*
  "Ти пройшла Темний Сектор!" — R2-JS захоплено пищить.
  "Жодна пастка тебе не зупинила!

  Але Темний Компілятор готує щось нове —
  він надіслав повідомлення через міжзоряний канал.
  Відповідь займе час... Треба навчитись АСИНХРОННОСТІ!"

  ➡️  Запусти: node chapter-07-pomylky/testy.js
*/

// ⚠️  НЕ ВИДАЛЯЙ!
module.exports = {
  bezpechneDialennia,
  bezpechnyiVyklyk,
  validateDzhedaia,
  GalacticError,
  zviazok,
  zonaNebezpeky,
};

//🎮 Симуляція: «Чат-бот технічної підтримки»**
// Скрипт аналізує змінну `userMessage`. Якщо там «ціна» — виводить прайс.
// Якщо “доставка” — терміни. В інших випадках — «Зв'яжу з оператором».
// Реалізувати через `switch`.

const userMessage = 'доставка';

switch (userMessage) {
    case 'ціна':
        console.log('Ціна 120 грн')
        break
    case 'доставка':
        console.log('Середній термін доставки 3 дні')
        break
    default:
        console.log(`Зв'яжу з оператором`)
}
